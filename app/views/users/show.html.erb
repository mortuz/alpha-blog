<h1>Welcome to <%= @user.username %>'s page</h1>

<div class="row">
  <div class="col-md-4">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>

<h4><%= @user.username %>'s articles</h4>

<%= render 'articles/articles', obj: @user_articles %>

<%= will_paginate @user_articles, list_classes: %w(pagination justify-content-end) %>