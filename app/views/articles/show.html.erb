<h1><%= @article.title %></h1>

<p class="lead">Description: <%= @article.description %></p>

<p>
  <%= link_to 'Edit', edit_article_path(@article), class: "btn btn-xs btn-info" %>
  <%= link_to 'Delete', article_path(@article), class: "btn btn-xs btn-danger", method: :delete, data: { confirm: "Are you sure you want to delete the article?" } %>
</p>

<p>
  <%= link_to '← Back to article listing', articles_path %>  
</p>